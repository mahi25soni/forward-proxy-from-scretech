# Setup Session 1 — 2026-08-15

## Done

- Defined docs layout: ROADMAP, journal, milestones, learning/
- Created `.cursor/rules/project.mdc` (always on) + `docs-workflow.mdc` (on `doc:` trigger)
- Agreed token discipline: journal checkpoints only, not every edit

## Learnedm

- `.mdc` = agent behavior; `docs/` = human notes — keep separate
- Docs workflow loads on demand via `doc:` prefix, not via globs
- Milestone MD written only after acceptance test passes

## Open

- M1 not started in code yet (handler still serves static files)

## Next

- M1 slice 1: recognize proxy-form request (absolute URL in request line)
- M1 slice 2: parse host/port/path from absolute URL
- M1 slice 3: upstream connect via getaddrinfo

## Commands

```bash
# M1 acceptance test (not passing yet)
curl -v --proxy localhost:8080 http://example.com
```
